/**
 * Assignment entity kept for reference only — no longer used in business logic.
 * Partners now have a direct manager_id foreign key on the partners table.
 * This file can be deleted once the database migration is confirmed complete.
 */
export {}; // empty module to avoid import errors
